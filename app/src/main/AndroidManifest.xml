<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inf3m171.adalto.appplayersm171">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FormularioUsuarioActivity" />
        <activity
            android:name=".ListaPlayersActivity"
            android:label="@string/title_activity_lista_players"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastraPlayersActivity"></activity>
    </application>

</manifest>